@model IList<FacultyViewModel>
@{
    ViewData["Title"] = "Index";
}

<h3>Факультети</h3>
<div class="row">
    <div class="col-12">
        <table class="table table-sm table-striped table-hover border-bottom">
            <thead>
                <tr class="d-none d-lg-table-cell">
                    <th>
                        Факультет
                    </th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="d-none d-lg-table-cell">
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.ActionLink("Редагувати", "Edit", "Faculty", routeValues: new { id = item.Id })
                            </td>
                            <td>
                                @Html.ActionLink("Видалити", "Delete", "Faculty", routeValues: new { id = item.Id }, htmlAttributes: new { @class = "btn-delete" })
                            </td>
                        </tr>
                    }
                }
                <tr>
                    <td colspan="3">
                        @Html.ActionLink("Додати факультет", "Create", "Faculty", null, htmlAttributes: new { @class = "btn btn-success" })
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>