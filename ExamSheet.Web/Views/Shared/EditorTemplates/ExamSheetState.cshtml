@model ExamSheet.Web.Models.ExamSheetState
@{
    var list = Html.GetEnumSelectList<ExamSheet.Web.Models.ExamSheetState>();
    var name = ViewData.TemplateInfo.HtmlFieldPrefix;
}

@Html.LabelFor(m => Model, "Статус")
@Html.DropDownList(string.Empty, list, htmlAttributes: new { @class = "form-control" })
